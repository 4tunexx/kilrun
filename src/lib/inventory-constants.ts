/** Default fraction of an item's original VP price refunded when resold.
 *  Runtime value comes from SiteSettings.inventoryConfigJson (admin Inventory tab). */
export const INVENTORY_RESELL_RATE = 0.5;
